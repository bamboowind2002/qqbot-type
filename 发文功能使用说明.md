# 发文功能使用说明

本文对应当前代码中的发文插件（`dist/plugin-article.js`、`dist/plugin-article-user.js`）。所有机器人指令都必须以 `-` 开头；管理员指令还可以使用完整写法。文章正文中的空白字符会在保存和发出时删除。

## 一、文章文件与基本规则

文章实际保存在：

```text
data/articles/text/<标题>.txt
```

默认文章根目录为 `data/articles`，也可以通过环境变量 `QQBOT_ARTICLE_DIR` 修改。

文章标题和分类名必须满足：

- 不能为空，不能是 `.` 或 `..`；
- 不能包含空格、换行、制表符等 Unicode 空白字符；
- 不能包含 `/`、`\`、控制字符；
- 最多 255 个 Unicode 字符。

保存文章时支持 UTF-8、UTF-16LE、GB18030、GBK、Big5 等输入编码，最终统一保存为 UTF-8，并删除正文中的所有 Unicode 空白字符。标题如果来自导入器，导入器会先删除标题中的空白字符再保存；手工上传标题若含空白字符则直接报错。

使用已有文章标题的指令会先进行精确匹配。精确匹配失败时，若标题包含匹配只有一项，会自动使用该完整标题继续操作；若有多项，则最多列出 50 项并显示实际匹配总数，提示使用完整名称。多项匹配时不会执行发文、选择或管理操作。

## 二、所有用户可用命令

### 1. 查看文章列表

```text
-文
-文 <关键词>
-文 <关键词> <页码>
-文章列表 [关键词] [页码]
```

每页显示 50 篇文章。关键词按标题包含关系匹配，页码从 1 开始。

### 2. 按分类查看文章

```text
-文 分类 <分类名>
-文 分类 <分类名> <关键词>
-文 分类 <分类名> <关键词> <页码>
```

分类查询只显示该分类目录中有效的 `.txt` 软链接。分类名不能包含空白字符。分类不存在或没有有效文章时显示暂无文章。

### 3. 选择当前文章

```text
-选 <标题>
-选择文章 <标题>
```

选择结果按用户保存。之后可以省略部分命令中的文章标题，使用当前选择的文章。

### 4. 设置或查看每段字数

发文命令中可以直接指定字数：

```text
-顺 300
-随 200
-乱 100
-难 150
```

每段字数必须是 10 至 2000 的整数。指定后会保存为该用户后续发文的默认字数。未指定时默认 100 字。

普通文章发文命令还可以在片段参数后使用 `|` 设置本次会话的成绩条件，例如：

```text
-顺 300 红楼梦 | 速度>=100 且 错字=0
```

这会在发出片段后保存对应的自动续段条件；条件为空时为无条件自动续段。

文章标题没有空格，因此需要指定文章时直接放在字数后面：

```text
-顺 300 红楼梦
-随 200 红楼梦
```

## 三、三种普通文章发文模式

### 1. 顺序发文

```text
-顺
-顺 <字数>
-顺 <字数> <标题>
-顺序发文 [字数] [标题]
```

从文章当前位置截取。位置按“用户 + 文章标题”保存，与群无关；不同用户互不影响。`-顺` 发送当前位置，`-下` 前进一段，`-上` 后退一段。文章发完后再次顺序发文会提示已经发完。

### 2. 随机段落发文

```text
-随
-随 <字数>
-随 <字数> <标题>
-随机段落发文 [字数] [标题]
```

在整篇文章中随机选择一个连续区间，不修改文章进度。

### 3. 全局随机选字

```text
-乱
-乱 <字数>
-乱 <字数> <标题>
-乱 <字数> <标题> <起点>-<终点>
-乱 <字数> <标题> <起点>~<终点>
-乱 <字数> <标题> <起点>至<终点>
```

下标范围从 1 开始，包含起点和终点。例如：

```text
-乱 20 红楼梦 1000-5000
-乱 20 红楼梦 范围=1000至5000
```

抽取的是不同有效行下标，不要求行内容不同。同一个行下标不会重复抽取，但相同内容可以因为位于不同下标而重复出现在结果中。随机拼接所选行，并按要求字数截断最后一行；结果中不包含换行。指定范围按有效行编号计算，范围内所有行的总字数必须不少于要求的字数。

不指定范围时，范围是整篇文章。此模式和随机段落模式都不保存进度。

## 四、难度发文

### 1. 难度等级

当前难度区间如下，区间为左闭右开：

| 等级 | 分数范围 |
| --- | --- |
| 淼 | 小于 0.1 |
| 水 | 0.1 至 0.3 |
| 易 | 0.3 至 0.8 |
| 普 | 0.8 至 5 |
| 难 | 5 至 15 |
| 虐 | 15 至 100 |
| 爆表 | 100 及以上 |

### 2. 命令

```text
-难度发文 <淼|水|易|普|难|虐|爆表> [字数]
-淼 [字数]
-水 [字数]
-易 [字数]
-普 [字数]
-难 [字数]
-虐 [字数]
-爆 [字数]
```

系统会从长度足够的文章中随机选择文章，再随机选择指定字数的连续片段并计算难度。只有实际落入目标区间的片段才会发送；每次最多尝试 300 次或 5 秒，找不到时严格报错，不发送近似难度的片段。负数难度结果不是“淼”，而是无效结果。

## 五、顺序进度

```text
-进
-进 +500
-进 -500
-进 =12345
-文章进度 [+/−/=数字]
```

不带参数查看当前文章位置。`+` 前进、`-` 后退、`=` 跳到指定下标位置。位置表示当前段起点，按 Unicode 字符计数，并自动限制在 `0` 到文章总字数之间。

注意：进度必须先选择文章；进度只对顺序发文有意义。

## 六、文章内搜索

```text
-搜 <关键词>
-搜 <关键词> <页码>
-搜 <关键词> 于 <文章标题>
-文内搜索 <关键词> [页码] [于 <文章标题>]
```

搜索结果返回匹配位置和上下文，每页最多 10 个结果。位置从 1 开始，支持 Unicode 字符。未指定文章时使用当前选择的文章。

## 七、发文消息格式与成绩续段

机器人发出的文章由三行组成：

```text
标题-难度分数
正文
-----第段号-共字数-触发人
```

没有标题时第一行只显示难度和分数。段号为 10001 至 99998 的随机数。当前实现会随机生成段号，但尚未实现会话内段号去重；成绩匹配以当前会话保存的最近一次段号为准。

### 1. 会话命令

发出任一种文章片段后会建立会话。会话按群聊中的“群 + 用户”或私聊中的“用户”区分，超时时间为 1 小时。

```text
-下
-下一段
-上
-上一段
-自
-自 <成绩条件>
-设置自动续段 [成绩条件]
-停
-结束发文
```

- `下` / `下一段`：顺序发文前进一段并发送，忽略当前成绩条件。
- `上` / `上一段`：仅顺序发文支持，回退一段并重新发出。
- `停` / `结束发文`：关闭当前会话。
- `自` 不带条件：恢复为无条件自动续段。
- `自 <成绩条件>`：设置成绩满足条件后自动发下一段。

自动续段的前提是：用户发送了与当前段号一致的成绩消息。只识别当前会话用户的成绩；段号不匹配、条件不满足或会话过期时不会续段。

### 2. 成绩条件

支持 `>`、`>=`、`<`、`<=`、`=`、`!=`，支持 `且`、`或` 和括号。例如：

```text
-自 速度>=100 且 准确率>=95%
-自 (速度>=100 且 击键>=5) 或 错字=0
-自 时间<=00:40
-自
```

成绩消息中的指标名称会按原文读取，例如 `速度166.73`、`击键6.59`、`码长2.37`、`键准82.15%`、`错字0`、`字数100`、`时间00:35.987`。时间可以是秒数，也可以是 `分:秒` 或 `时:分:秒` 格式。百分数条件应带 `%`，程序会按百分数数值比较。

## 八、管理员文章管理

当前管理员 QQ 为 `1144107042`。管理员命令可在私聊或群聊使用；普通用户发送管理命令不会执行管理操作。

### 1. 上传或更新文章

```text
-管 传 <标题>
-管 上传 <标题>
```

消息中附加 `.txt` 文件，或者引用一条包含 `.txt` 文件的消息。文件会被下载、识别编码、删除正文中的空格、制表符等非换行空白，并将换行统一为 `\n` 后保存为 UTF-8。标题已存在时覆盖正文，不存在时新建。

### 2. 正则替换文章内容

```text
-管 改 <起点> <终点> <标题>
下一行：正则表达式
第三行起：替换文本
```

例如：

```text
-管 改 1 100 红楼梦
林黛玉
黛玉
```

替换区间按紧凑正文的 Unicode 字符下标计数（不计算换行），从 1 开始且包含起止位置，最多 10000000 个字符。正则使用 JavaScript Unicode 正则；替换后仍按 UTF-8 保存并保留未涉及的换行。

### 3. 删除文章

删除使用两步确认，确认码有效期 5 分钟：

```text
-管 删 <标题>
-管 确认 <确认码>
```

删除时会先清理文章所属的全部分类软链接，再删除正文文件。

### 4. 分类软链接管理

正文只保存一份，分类目录不复制正文：

```text
data/articles/text/四季.txt
data/articles/categories/散文/四季.txt -> ../../text/四季.txt
```

管理员命令：

```text
-管 分类 添加 <分类名> <文章标题>
-管 分类 删除 <分类名> <文章标题>
-管 分类 列表
-管 分类 难度列表
-管 分类 难度查看 <分类名>
-管 分类 难度启用 <分类名>
-管 分类 难度停用 <分类名>
```

同一篇文章可以加入多个分类。添加和删除分类只操作软链接，不改变正文、标题、用户进度或难度数据。删除文章会自动移除所有分类中的对应链接。分类列表会显示每个分类及其有效文章数。

分类还可以控制难度发文取样：一篇文章只要属于至少一个已启用分类，就会进入难度发文池；新分类默认停用，已有分类迁移后默认启用。分类开关不会改变普通分类查询。

## 九、文章导入器

导入器位于 `article_import/importer.js`，默认调用极速中文网文本列表接口：

```bash
node article_import/importer.js
```

当前默认限制：单次最多 1000 篇、正文总大小最多 20 MiB、请求间隔 200ms。导入过程会在标准输出显示进度：

```text
[23/1000] 标题 | 成功 23 | 失败 0 | 123456 字节
```

无参数时使用文本列表接口；也可以传入排行榜页面 URL，走排行榜详情页兼容路径：

```bash
node article_import/importer.js https://www.jsxiaoshi.com/result_rank.html
```

导入器会：

- 获取标题和正文；
- 删除标题中的空白字符后再保存；
- 保存正文为 UTF-8 并删除正文空白；
- 单篇失败时记录失败原因并继续处理其他条目；
- 将来源和原始标题写入 `data/articles/import-sources.json`；
- 不会把导入数据和来源清单加入 Git。

## 十、本地测试 CLI

本地 CLI 位于 `article_cli/cli.js`，不连接 NapCat，也不连接 MySQL，适合检查本地文章、模式和难度候选：

```bash
node article_cli/cli.js
```

CLI 中输入 `帮助` 查看命令，输入 `退出` 结束。主要命令与机器人类似：

```text
文 [关键词]
选 <标题>
进 [+/−/=数字]
顺 [字数]
随 [字数]
乱 [字数] [起止范围]
难 <难度> [字数]
搜 <关键词>
```

CLI 的顺序进度只保存在本次进程内，不写入数据库。

## 十一、常用操作示例

```text
-文
-选 红楼梦
-顺 200
-进
-进 +1000
-自 速度>=100 且 错字=0
```

另一个用户可以独立使用：

```text
-随 100 红楼梦
-乱 30 红楼梦 2000-5000
-难 普 150
-停
```

管理员初始化分类的典型流程：

```text
-管 传 红楼梦
-管 分类 添加 古典 红楼梦
-管 分类 添加 小说 红楼梦
-文 分类 古典
```

## 十二、当前边界与注意事项

- 文章正文、分类软链接和导入来源都属于本地运行数据，不应提交 Git。
- 分类查询不会复制正文；移动或删除正文文件会使对应链接失效，正常删除文章应使用管理员删除命令。
- 难度发文保证算法会尽力寻找目标区间，但候选不足或计算超时情况下可能返回最接近的片段。
- 自动续段只在成绩消息匹配当前会话和当前段号时触发，不会因为普通聊天消息自动发文。
- 未启动机器人时不要用 `npm start`、`npm run dev` 或 PM2 做测试；优先使用测试套件和本地 CLI。
