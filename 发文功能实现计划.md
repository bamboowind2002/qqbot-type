# 发文插件分步实现计划

## 总体方案

新增独立发文插件，所有指令以 `》` 开头。高频操作使用单字命令，首次默认每段 100 字，之后记住用户上次字数。较长命令作为兼容别名保留，但帮助文档主推短命令。

按功能点逐步交付：

1. 文章存储与管理员上传。
2. 查询、选文、搜索和进度。
3. 三种普通发文模式。
4. 成绩识别与自动续段。
5. 粗粒度难度地图与难度发文。
6. 极速打字网测试语料导入。
7. 生产迁移与部署。

## 短命令设计

### 选文与查询

| 命令 | 行为 |
|---|---|
| `》文` | 显示文章列表 |
| `》文 关键词` | 筛选标题 |
| `》文 关键词 2` | 查看第 2 页 |
| `》选 标题` | 选择当前文章 |
| `》进` | 查看当前文章总字数、进度和百分比 |
| `》进 +500` | 快进 500 字 |
| `》进 -500` | 快退 500 字 |
| `》进 =12345` | 将进度设到第 12345 字 |
| `》搜 关键词` | 在当前文章搜索关键词并分页返回位置和上下文 |
| `》搜 关键词 于 标题` | 不切换当前选文，直接搜索指定文章 |

搜索只返回位置，不自动修改进度；用户再用 `》进 =位置` 跳转。

### 普通发文

| 命令 | 行为 |
|---|---|
| `》顺` | 当前文章按进度发文，使用上次字数 |
| `》顺 200` | 当前文章顺序发 200 字，并记住 200 |
| `》顺 200 标题` | 不必先选文，一次指定字数和文章 |
| `》随` | 从当前文章随机截取连续段落 |
| `》随 200 标题` | 从指定文章随机截取 200 字 |
| `》乱` | 从当前文章全文随机抽取上次字数个不同下标 |
| `》乱 200 标题` | 从指定文章全文抽取 200 个不同下标 |
| `》乱 200 标题 1000-5000` | 从指定文章的第 1000–5000 字下标范围内抽取 200 个不同下标 |
| `》淼`、`》水`、`》易`、`》普`、`》难`、`》虐`、`》爆` | 按对应难度和上次字数发文 |
| `》水 200` | 按“水”等级发 200 字 |
| `》难 200` | 按“难”等级发 200 字 |
| `》上` | 手动回到上一段 |
| `》下` | 忽略成绩门槛，提交当前段并发下一段 |
| `》停` | 关闭当前会话 |

参数解析规则：

- 字数范围为 10–2000。
- 省略字数时使用用户上次设置，首次默认 100。
- `顺/随/乱` 中，数字后的文本首先解析末尾的可选乱序范围，其余文本视为标题；文章标题禁止包含任何空白字符。
- `淼/水/易/普/难/虐/爆` 本身就是难度发文命令，后面只能跟可选字数，不再重复填写难度；其中 `爆` 对应完整等级名“爆表”。
- 随机段落和随机选字要求文章长度不少于请求字数；顺序模式允许最后一段不足设定长度。
- `乱` 随机产生互不重复的字符下标，按随机抽取顺序输出对应字符；字符内容允许重复。范围格式为 `起点-终点`、`起点~终点` 或 `起点至终点`，按 1-based 且包含两端处理；范围内可用下标不足时返回错误。

### 完整命令别名

短命令和长命令进入同一个解析与执行入口，权限、回复和错误提示完全一致。普通命令仅替换命令名称；难度短命令本身携带等级，转换为完整命令时把该等级补入参数：

| 短命令 | 完整命令 | 示例 |
|---|---|---|
| `》文` | `》文章列表` | `》文章列表 关键词 2` |
| `》选` | `》选择文章` | `》选择文章 中秋节想你的心情` |
| `》进` | `》文章进度` | `》文章进度 +500`、`》文章进度 =12345` |
| `》搜` | `》文内搜索` | `》文内搜索 关键词 于 文章标题` |
| `》顺` | `》顺序发文` | `》顺序发文 200 文章标题` |
| `》随` | `》随机段落发文` | `》随机段落发文 200 文章标题` |
| `》乱` | `》随机选字发文` | `》随机选字发文 200 文章标题` |
| `》淼`、`》水`、`》易`、`》普`、`》难`、`》虐`、`》爆` | `》难度发文 <淼\|水\|易\|普\|难\|虐\|爆表> [字数]` | `》难 200` 等价于 `》难度发文 难 200` |
| `》上` | `》上一段` | `》上一段` |
| `》下` | `》下一段` | `》下一段` |
| `》停` | `》结束发文` | `》结束发文` |
| `》自` | `》设置自动续段` | `》设置自动续段 速度>=150`；不带条件表示无条件续段 |
| `》管 传` | `》文章管理 上传` | `》文章管理 上传 文章标题` |
| `》管 改` | `》文章管理 替换` | `》文章管理 替换 1 10000 文章标题` |
| `》管 删` | `》文章管理 删除` | `》文章管理 删除 文章标题` |
| `》管 确认` | `》文章管理 确认删除` | `》文章管理 确认删除 123456` |
| `》管 索` | `》文章管理 同步难度地图` | `》文章管理 同步难度地图` |
| `》管 状` | `》文章管理 难度地图状态` | `》文章管理 难度地图状态` |
| `》管 取消` | `》文章管理 取消建图` | `》文章管理 取消建图` |

帮助信息同时显示短命令和完整命令，但把短命令放在首位。测试需逐项验证每对别名得到相同的解析结果，避免两套命令行为分叉。

### 自动续段

每次发文都会建立自动续段会话，默认无条件自动续段。需要成绩门槛时，使用竖线将发文参数和条件隔开：

```text
》顺 100
》顺 100 | 速度>=150 且 击键>=6
》水 100 | 速度>=150 且 (击键>=6 或 键准>=95%)
```

- 不写 `|`：收到当前段对应成绩后无条件自动续段。
- 只写 `|`：与省略 `|` 相同，仍按无条件自动续段处理。
- `|` 后有表达式：成绩满足表达式才续段。
- 会话开始后可用 `》自` 清除门槛，恢复无条件自动续段。
- `》自 条件表达式` 修改当前会话的门槛。
- 不提供“关闭自动但保留会话”的状态；不再需要续段时使用 `》停` 结束会话。

成绩条件支持任意指标名、`> >= < <= = !=`、`且`、`或`和括号。解析成绩中的任意“名称+数值”字段，不维护固定指标名单；百分比按百分数比较，时间统一换算为秒，缺失指标的比较结果为假。

只有同一用户在同一群或私聊中提交、且段号与当前段一致的成绩才有效。会话一小时无活动自动失效：

- 顺序模式在成绩合格或用户执行 `》下` 后提交进度。
- 自动续段等待期间仍可使用 `》下` 跳过当前门槛并立即发下一段，或使用 `》上` 回到上一段起点；人工翻段后只接受新当前段号的成绩。
- 随机和难度模式不保存阅读进度，合格后重新抽取。
- 顺序模式最后一段完成后关闭会话并保留完成位置。

### 管理员短命令

沿用管理员 QQ `1144107042`：

| 命令 | 行为 |
|---|---|
| `》管 传 标题` | 上传或更新文章 |
| `》管 改 开始 结束 标题` | 对指定字符区间执行正则替换 |
| `》管 删 标题` | 请求删除并返回确认码 |
| `》管 确认 确认码` | 确认删除 |
| `》管 索` | 增量同步所有新增、未建图或内容已变动文章的难度地图 |
| `》管 状` | 查看地图任务、文章覆盖率、临时缓存和存储状态 |
| `》管 取消` | 取消当前难度地图任务 |

上传兼容两种方式：

1. 文件和 `》管 传 标题` 放在同一消息。
2. 先发送文件，再引用文件发送命令。

正则替换命令后：

- 第二行填写正则。
- 第三行起填写替换文本，允许空替换。
- 起止位置均从 1 开始且包含端点。
- 单次区间最多一千万字符。
- 在限时、限内存子进程执行，15 秒超时。

## 存储与实现

- 正文保存为 `data/articles/text/标题.txt`，目录内只放 UTF-8 `.txt`。
- 标题禁止路径分隔符、控制字符和 `.`/`..`，最长 255 个 Unicode 字符。
- 上传采用流式编码检测和转换，支持 BOM、UTF-8、GB18030/GBK、Big5、UTF-16，并允许管理员显式指定编码。
- 转换后删除全部 Unicode 空白字符和 BOM。
- 单篇文章不限制大小，规范化后文章总量限制为 10 GiB；操作时保证磁盘至少保留 10 GiB。
- 每篇文章建立字符到字节的偏移检查点，使发文、随机取字、关键词搜索和区间替换不必将整篇文章载入内存。
- 上传和修改先写临时版本，校验、生成检查点后原子替换；失败恢复旧文章。
- 同一标题的上传、替换和删除串行执行。
- 内容更新后：
  - 重置该文章全部用户进度。
  - 关闭相关活动会话。
  - 使该文章的难度地图和精确候选缓存失效；其他文章不受影响。
  - 管理员回复中显示被重置的进度数量。

新增 MySQL 表：

- `article_catalog`：标题、字符数、字节数、SHA-256 内容修订号、已建图修订号、建图状态、更新时间。
- `article_progress`：QQ、标题、已提交位置。
- `article_user_settings`：当前选文和上次发文字数。

## 发文与难度地图

发文固定为三行：

```text
标题-水0.11
无空白正文
-----第47884段-共111字-@触发人
```

- 无标题时第一行只显示难度和分数。
- 难度调用现有 `get_rank()`，保持当前等级边界。
- 字数按 Unicode 码点计算。
- 触发人使用群昵称、昵称、QQ号依次回退，只输出普通文本。
- 段号在 `10001–99998` 中随机生成，同一会话一小时内不重复。

难度地图与按需验证：

- 不再为 10–2000 的每个字数分别建立永久索引。`》管 索` 只维护一份与请求字数无关的粗粒度难度地图，不接受标题参数。
- 每次同步先比较文章的“内容修订号”和“已建图修订号”：只读取并处理新增、从未建图或内容已变动的文章；修订号相同的文章不重新读取、不重新计算。已从文章目录删除的标题会同步移除地图记录和精确候选缓存。
- 建图时以 100 个 Unicode 字符为基础块，按文章字符数进行分层随机抽样，而不是记录所有可能区间。全库最多保留 100,000 个基础块，单篇最多 5,000 个；短文章至少抽取一个有效块。抽样使用可复现随机种子，重复增量建图不会无故重排全部记录。
- 每个抽样块调用 `get_rank()` 得到粗粒度难度；地图记录只保存标题、文章修订号、起点、实际长度、难度和分数，不复制正文。建图工作量和地图大小因此受固定上限约束，不随 10–2000 的可选字数种类增长。
- 新增或变动文章使全局记录超过 100,000 条时，只对已保存的地图记录做确定性下采样以腾出配额，不重新扫描未变文章；处理中的单篇结果先写临时文件，完成后再原子替换该文章旧地图并更新“已建图修订号”。
- 用户请求任意长度时，先根据目标等级从相同或相邻难度的基础块中选择区域；请求超过 100 字时结合覆盖范围内多个基础块的分数选择区域，并在附近随机扰动起点。
- 对每个截取出的准确字数调用 `get_rank()` 重新计算。400 字和 401 字会分别计算实际难度，但共用同一份难度地图。
- 单次请求最多尝试 300 个候选或运行 5 秒，以先达到者为准；一旦找到目标等级即可提前发送。若没有精确命中，则发送本次已评估候选中最接近目标难度的一段，不报“难度不足”。
- “最接近”按分数到目标等级区间的距离判断：落在目标区间内距离为 0，区间外取到最近边界的差；“淼”取最小分数优先，“爆表”取最大分数优先，同距离时随机选择。
- 发文首行始终显示该段通过 `get_rank()` 得出的实际等级和分数，不把近似结果标成用户请求的等级。
- 成功候选进入按“字数+实际等级”区分的内存 LRU 缓存，每组最多 200 条、全局最多 50,000 条。缓存只保存标题、修订号、起点和分数；取用时再次核对修订号并重新计算实际难度。
- 难度地图不存在或覆盖不完整时，从所有长度足够的文章中均匀选择随机位置作为候选，并继续使用相同的精确优先、最近难度回退流程；`》管 状`仍显示地图覆盖率，`》管 索`用于改善命中率和响应速度，而不是难度发文的硬前置条件。
- 只要语料库中至少有一篇文章达到请求字数，难度命令就必须发出一段；仅在没有任何足够长的文章或正文读取失败时返回错误。
- 候选计算放在子进程中，不阻塞机器人消息循环。
- 建图使用全局单并发子进程，支持修订号驱动的增量同步、状态查询、取消、超时和异常临时文件清理。取消后，已经原子提交的文章保持有效，未完成文章继续标记为待同步。

## 测试与交付

- 编码转换、空白清理、10 GiB 总配额、路径安全、原子更新和失败回滚。
- 短命令解析、非法空白标题、默认 100 字、记忆上次字数，以及七个难度直达命令与完整命令的等价性。
- 三行格式、昵称回退、Unicode 字数、顺序尾段、随机边界、指定范围、乱序下标不重复和字符值可重复。
- 跨群共享进度、成绩合格后提交、上下段、快进快退、文章更新重置。
- 默认无条件自动续段、条件续段、自动等待期间人工上下段，以及任意成绩指标、百分比、时间、且/或/括号、缺失指标、错误用户、错误群、错误段号和会话超时。
- 难度等级边界、100 字基础块地图、400/401 等相邻长度共用地图但独立计算、精确命中优先、最近难度回退、实际难度展示、无地图随机兜底、尝试时限、LRU 上限、只处理新增或变动文章、删除同步、未变文章零重算和建图取消恢复。
- 核心模块使用临时目录和模拟 NapCat/MySQL 测试，不连接真实服务。

极速打字网主页已有选择文本、发文字数和自动换段功能：[页面入口](https://www.jsxiaoshi.com/)。实现时检查其公开、无需登录的请求，限速为每秒一次，最多导入 10 篇、总计不超过 20 MiB；语料和来源清单仅保存在服务器运行目录。若没有稳定公开接口，则手动导出少量文章并通过 `》管 传` 导入。

最终部署包括幂等数据库迁移、运行目录创建和新依赖安装。只有得到明确部署授权后才更新远端并重载 PM2；不需要重新编译词提原生模块。
